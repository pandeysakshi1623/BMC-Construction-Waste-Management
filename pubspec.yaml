name: smart_waste_monitor
description: Smart Construction Waste Monitoring System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.1
  image_picker: ^1.1.2
  geolocator: ^12.0.0
  mobile_scanner: ^5.2.3
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.0
  qr_flutter: ^4.1.0
  share_plus: ^10.1.4
  path_provider: ^2.1.4
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

